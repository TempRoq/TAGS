using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticValueSetter : MonoBehaviour
{
    public GameObject[] spawnChars;
    public float currMeter;
    public float maxMeter;

    public CharSwitchManager.MainCharacter startingCharacter;

    public float paintExplosionDelay;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
